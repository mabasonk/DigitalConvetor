using System;

namespace DigitalConvetor
{
    class Program
    {
        static void Main(string[] args)
        {
             Console.WriteLine(DecimalToWordHelper.ToWords(112.01));
            // Console.WriteLine(DecimalToWordHelper.ToWords(112.01));
            //Console.WriteLine(DecimalToWordHelper.ToWords(-22));
            // Console.WriteLine(DecimalToWordHelper.ToWords(-1001));
            //Console.WriteLine(DecimalToWordHelper.ToWords(0.00));
            // Console.WriteLine(DecimalToWordHelper.ToWords(0));
            Console.ReadKey();
        }
    }
}
